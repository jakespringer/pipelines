"""Emergent-misalignment (``em``) example — a mockup of how the ``em``
experiment group would be wired under the current `pipelines` design.

See README.md. Nothing here runs (the framework isn't built yet); this is a
worked example of the authoring style.
"""
